name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'  # Adjust based on your local Python version
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Set environment variable to disable GPU
      run: echo "CUDA_VISIBLE_DEVICES=" >> $GITHUB_ENV
    - name: Run tests
      run: |
        pytest
    - name: Set environment variables to suppress TensorFlow warnings
      run: |
          echo "CUDA_VISIBLE_DEVICES=" >> $GITHUB_ENV
          echo "TF_CPP_MIN_LOG_LEVEL=3" >> $GITHUB_ENV  # Suppress TensorFlow logging (0=debug, 1=info, 2=warning, 3=error)
   
